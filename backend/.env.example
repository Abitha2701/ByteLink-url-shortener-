# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/bytelink

# Server port
PORT=4000

# JWT secret for signing tokens (change this in production)
JWT_SECRET=change_this_to_a_strong_secret
